# In production, set DATA_DIR to a writable persistent path or mount /app/data as a volume.
# If both ADMIN_PASSWORD and ADMIN_PASSWORD_HASH are set, the hash is used.
DATA_DIR="./data"
ADMIN_EMAIL="admin@moviejet.org"
ADMIN_PASSWORD="change-this-before-deploying"
ADMIN_PASSWORD_HASH=""
SESSION_SECRET="replace-this-with-a-long-random-secret"
